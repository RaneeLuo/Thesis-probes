# Thesis State Document — v3
**Project:** Diagnostic Evaluation Framework for Time-Series–Text Alignment (TU/e DS&AI Master's Thesis, Ranyi/Ranee)
**Last updated:** 2026-09-01 (rev. 23 — **CHAPTER 1 WRITTEN AND CHECKED**: all six sections drafted by Ranyi in Overleaf from template v2, section-by-section check cycle complete. UK spelling decided (watch "-ize" strays). Contribution reconciliation vs the proposal EXECUTED and closed: P2+P4(a) claimed via the item-construction clause in contribution 1; P4(c) closes with one definitional sentence in 2.4 (training-time vs evaluation-time hard negatives — ADOPTED, to write); T3/T4/T5 exceed the proposal (reimplementation was unplanned — proposal said "clone repositories"); revised proposal drops "state-of-the-art" from §8 item 3, thesis follows revised. In-writing verifications: 0.997/0.999 vs finding_metric_saturation.md; the 99.7%-on-our-pool reporting rule — **ch1 prose file violates it, one-line fix pending**; 1.5's order-invariant-scarcity phrasing vs ch5 prose. Carried to-dos for the Ch.2 session: 2.4 sentence; MMTS/TS-Haystack bib arXiv IDs; **2.1 must carry the dataset counts (Ch.1 now has none — moved out of 1.4.2)**; prose-file fix; Ranyi's assembled-chapter checklist pass in Overleaf. Handoff (xx) holds the full record) — previously (rev. 22 — template stage closed: thesis_final_template.md v2 is the agreed final writing template; sub-questions RQ1–3 adopted; appendices repackaged A–D; heading naming "Negative-control text-embedding baseline"; conclusion assembly rule 1.5+5.6+5.7; front matter incl. conduct declaration + confidentiality Public. Previous: rev. 21 — same-session addendum: **AUTHORSHIP: prose files are reference material; Ranyi writes the thesis herself in Overleaf** (serves the assessment form's independence-in-writing criterion). Four flagged decisions closed per advice: contribution check while writing; **three-signature table added to ch5 5.1** (source §6 re-read; disclosed ChatTS-C2 extension); **Fons/Tan note placed in ch6 6.1**; ⟦E-bytes⟧ no note. **Supervisor agrees with all decisions — FW-1 stays future work; SCOPE CLOSED FOR GOOD**; the FW-1 pending marker in ch6 6.3 is superseded/deletable. Claude's role forward: support on request — appendix source packs, checks, defense prep) — previously (rev. 20 — **THESIS PROSE CONVERSION COMPLETE: every chapter converted from content draft to official prose in one session** (files delivered to Ranyi; suggest committing under docs/thesis_prose/): conversion plan + ch1 intro, ch2 models, ch3 methodology, ch4 results ×4 sections, ch5 synthesis, ch6 discussion **+ NEW 6.4 Conclusions** (no draft existed; assembled strictly from S.6 + contributions, zero new numbers, proposal status). Framing decisions (Ranyi): **NO TU/e CS template exists** — target = official writing guideline (standard structure, 40–60 pp, QANU criteria + assessment form) in her university Overleaf style; **delivery = plain prose per chapter, Ranyi does all LaTeX** (skeleton built, compile-tested, retired same session; references_skeleton.bib kept, verified-fields-only); page budget 47–54 pp; appendix policy A–F. **S.5 DECIDED: the 25 named OUTCOME predictions (15/10) are the tally, boundary sentence stated; validation-stage (~13 narrative-arc IDs) and per-command layers reported separately** — the rev. 19 open question is closed; corollary suggestion: ⟦E-bytes⟧ needs no exclusion note under this boundary (confirm at correction). Standard provenance sentence in every results section. **Substantive correction found in conversion: floor C1** — the draft's 'at or below chance on every component' is wrong for C1 (swap 0.576 [0.512, 0.644] significantly above chance; VOID by argument, margin 0.005; probe1_findings_embedding_floor.md read IN FULL, closing its re-read flag); prose 4.4.1 accurate; below-chance random cells (C3 0.221 / C5 0.405) restored to prose. **Shorthand trap defused:** TRACE order census re-verified from source = {dependent: 2005, ambiguous: 1}, ZERO invariant — handoff (vi) '2,005/1/0' is not in the TRUCE dep/inv/amb order; drafts correct. FW treatments verified vs the scope record read fresh (FW-1 pending marker preserved; FW-2 never-negative-control; FW-3 one sentence; FW-4 absent). ChatTS no-transfer placement implemented: full 2.5 + 4.3.4 restatement + 6.2 (confirm at correction). Session verification: noise floor vs phase1a line 78 digit-exact; TRACE ladder ratios re-derived (pool-2,006 chance ≈0.00408); census chains 738+125=863, 400→389→344=11.6%; saturation 4/139 / 245/7,334; the 0.94-vs-0.96 Align 'discrepancy' resolved by the W-1 record's own quotable-form pin (not an error). NEXT: Ranyi correction pass over 8 files; contribution list vs proposal; three-signature-table option (5.1); Fons/Tan sentence placement (6.1/6.2); FW-1 supervisor line; then front matter, appendices A–F, numbering pass, length check. Log 2026-08-21 holds the full arc incl. Claude environment disclosure) — previously (rev. 19 — **THESIS WRITING STAGE COMPLETE AT CONTENT LEVEL.** All chapters content-drafted in one session and committed under `docs/thesis_drafts/`: background/intro (B.1–B.5), models & reproduction (MR.1–MR.7), methodology (M.1–M.8), four results chapters (CLaSP R1, TRACE R2, ChatTS R3, floor R4), the cross-model synthesis chapter (S.1–S.6: comparison rules, graded diagnostic × model matrix with all cells source-verified digit-level this session, four shortcut profiles, cross-cutting findings, 25-prediction ledger 15/10, scope of claims), and discussion/limitations/future-work (D.1–D.3, FW-1 pending-supervisor marker intact). **Terminology binding decision: "diagnostic" replaces "probe" in all thesis prose** (supervisor-flagged collision with probing classifiers; repo artifact names unchanged; mapping note in thesis; PROJECT_CONTEXT row added). **W-1 literature queue CLOSED via alphaXiv** (record: `docs/w1_verification_record.md`): MMTS-Bench two ⚠ claims verified-with-precision (240 = the Align subset of 2,424; 0.94–0.99 Align ceiling; prefix ablation Table 7 Sem→TS row digit-exact), TS-Haystack venue claim DROPPED (arXiv preprint, no venue; now v6, 4 datasets/4 modalities confirmed), ChatTS RQ5 inversion verified qualitatively (values in a bar figure — quote the sentence, no digits; Dataset A 0.889/0.788 digit-exact), BEDTime authors exact; plus the saturation-doc citation pair (Reimers&Gurevych + Ethayarajh) verified, cite jointly. **§6.1 BEDTime row CORRECTED with sign-off** (five datasets incl. NICU-HR per v3 — the earlier fabricated-note applied to an earlier paper version; zero thesis impact, uses survive). Supervisor email status: not required for now per Ranyi; FW-1 remains pending. NEXT: template arrives → convert content drafts to official prose; S.5 tally framing question open [25 named vs grand total]) — previously (rev. 18 — **ChatTS ARM EXECUTED, SCORED, AND CLOSED — THE PROBE × MODEL MATRIX IS COMPLETE.** GPU session (rented A100): 31,356 responses, all pod gates green; error #17 gate-caught (weight-bytes constant = tensor bytes, real file bytes 29,750,198,302; plus the un-landed-fix catch — commit message claimed the constant change, HEAD didn't carry it; §2b). Analysis off-pod, all structural gates GA1–GA9 green against registered values. Scorecard: **PC1-1 MISS** (SUSHI random 0.726 [0.695, 0.755] vs ≥0.90; NOT VOID; C3 stratum VOID), **PC1-2 HIT** (pooled P(A) 0.442), **PC1-3 HIT** (600/600), **PJ HIT flat both substrates** (TRUCE ci90 [−0.025, +0.021] — masking quotable, frozen-prefix rerun not fired), **A≈C HIT**, **anchor cond_C = stock gaussian EXACT** (letter agreement 1.0000 both substrates — manual path proven at results level), ⟦E-time⟧ MISS (0.44 h vs 1–3 h band). **TRUCE ruled WEAK-VIABLE** (0.622 [0.591, 0.654] vs the 0.60 convention — borderline argued, Ranyi's call). **The registered NON-prediction answered: C2 trend-family COLLAPSES TO CHANCE (swap 0.527 vs random 0.852, Holm 5.6e-30) while C4 degrades only partially (+0.097)** — C1/C5 flat, C3 VOID. Probe 2: SUSHI sf_all −0.267 to below chance; **two-level shuffle finding: within-patch FLAT vs across-patch −0.219 — order is read at the patch-arrangement level**; TRUCE ex_half worst (−0.174), masking flat (PJ-defended); TRUCE sf_all DiD +0.335 [0.143, 0.550] with >half carried by the thin invariant leg (n=18) — dep drop −0.113 is load-bearing; degenerate constant letter-perfect ×8. Probe 3: **NO SHORTCUT — every sub-order rung's CI contains chance on both substrates; five-number inert (−0.261/−0.103 vs rung 1); cond_B ≈ rung 1 (prefix content inert)**; contrasts: TRUCE 4/4 equivalences certify at ±0.05, SUSHI two legs inconclusive-by-width at n=140 (pre-named width-vs-shift distinction). Thesis sentence: **ChatTS's matching is carried entirely by ordered structure — the first model with no Probe-3 shortcut — beside a component-specific Probe-1 blind spot (trend family)**. Full record §2; handoff header (xv) + §4.9) — previously (rev. 17 — **ChatTS LOCAL PREPARATION COMPLETE** (2026-08-15/16): the paper-era checkpoint revision PINNED (`1e661101dcfff86dc66f3397336b85f2f1cc5e89`) after the in-place 0801 weight replacement on HF main was discovered (era diffs source-verified: 2-field vs 7-field prefix incl. order-sensitive endpoints, patch 16 vs 8, ts max_length 2048 vs 32768); all three MCQ manifests BUILT AND GATED on CPU (Probe-1 11,080 rows, Probe-2 1,756 rows with frozen seeded distractors and certified-group hard joins, Probe-3 3,912 rows incl. the five-number rung [posable only for ChatTS, sha 4a4b3475f9e7], donor-prefix cond_B, and the pre-registered PJ prefix-jitter control); the perturbation module tested on all 386 real test signals (M1-C masking = survivors'-mean fill = exact model-level fill-0, identity gated; the SUSHI constant a full identity control in all 8 conditions; drift census: TRUCE prefix drift median 10% of std — PJ is LOAD-BEARING for TRUCE masking); the MANUAL ENCODING PATH proven bit-exact against the stock processor on 1,756/1,756 rows (arithmetic imported from the checkpoint's own file); GPU runner + `docs/chatts_gpu_runbook.md` delivered (31,356 questions, logit readout with pre-named generation fallback, smoke stage hard-gating splice arithmetic ⟦SUSHI +126/TRUCE −1⟧ and weight bytes 29,749,997,568; ~3–5 h, ~€15–40). Predictions PC1-1..3 registered + explicit NON-prediction on the component ordering/C4. Error #16 gate-caught, §2b. The GPU session is pure execution; handoff §4.9 holds the full record) — previously (rev. 16 — TRACE Probe-3 arm EXECUTED, SCORED, AND CLOSED: **P3-2a CONFIRMED** (TOST resample~sf_all at ±0.05, 90% CIs ±0.005–0.010, all seeds, dep n=2,005 text→ts; Wilcoxon dominance 48–50% — a coin flip) and **P3-3 CONFIRMED** (resample retains 2.52/2.58/3.04× global chance, ratio CIs exclude 1 everywhere, inside the registered 2.0–3.5× band) — **the Probe-2 residual bridge is CLOSED: the 2.9–3.1× order-free residual is carried by value-DISTRIBUTION SHAPE** (exact multiset → i.i.d. same-distribution draw changes nothing; matched noise kills it to 1.36×, two of three gaussian MRR CIs touching global chance). **The anchor investigation returns the opposite of the CLaSP finding: TRACE does NOT exploit the length channel** (gaussian position chance→ceiling ≈ 0 in both main strata; frac(rank≤544) 0.276–0.292 vs uniform 0.271; rare-length stratum position +0.01..+0.04 against a 0.2658 composite ceiling) — the CLaSP-TRUCE length-floor phenomenon does not transfer. **P3-6 MISSED mildly** (seed 14 primary resample between-stratum CI excludes 0 at +1.7 pts; secondary excludes 0 in all seeds — a percent-scale offset at a 98% ceiling, NOT a Probe-2-style mixture: the ladder ordering sf_all≈resample>gaussian holds in every stratum/seed/direction). **Seed-44 arbitration RESOLVED: no significant direction in any seed at n=2,005** — CLaSP's P3-2b reversal reads as a thin-n anomaly (n=135), stays flagged there, cross-model verdict equivalence. Mechanics: renorm-always construction (measured commutation ≤1e-6), joint-channel resample with row-level redraw (census 41/25/42, HARD-gated diagnostic↔runner, hit exactly), per-channel gaussian (provably ≡ standard post-renorm), dead ptp==0 pass-through ×498 gated. Probe 3 remains open ONLY for ChatTS) — previously (rev. 15 — FLOOR Probe-3 arm EXECUTED AND SCORED: **P3-7 CONFIRMED** (12/12 TOSTs at ±0.05 abs MRR, 90% cluster-bootstrap CI; max inference-cell |Δ| 0.0067; scoring pinned pre-run; REF cross-computation gate 4/4 in every arm; JG join digit-exact vs the committed floor Probe-2 records) — the Probe-3 pipeline does not manufacture degradation on a no-capability model; $0.45 spent once (2,310 surrogate strings), cache-deterministic ($0 rerun reproduced arm-42 query 739's rank move 298→273 exactly); error #15 recorded (delivered-script G8-rank false inference — rank gated under whole-pool replacement; the Probe-2 “G8 is embedding-identity only” correction not carried; §2b ledger); thin-cell DEFLATION observation (truce/invariant 0.071→0.015–0.028 under resample, ambiguous 0.206→0.007–0.106 — P2-4's mimicry showed thin-cell INFLATION; both directions now demonstrated, decomposition and n-reporting mandatory); Probe 3 remains open for TRACE + ChatTS) — previously (rev. 14 — Probe 3 DESIGNED (Q1–Q5, handoff §4.8) and the CLaSP arm EXECUTED, SCORED, CROSS-VERIFIED: **P3-4 CONFIRMED** (resample retains 2.2–2.3× chance, CI>1 all seeds); P3-1/2b/2c/5 MISSED with mechanisms recorded — the gaussian anchor is reframed as the LENGTH-ONLY FLOOR (TRUCE fit quantitative; SUSHI side open); headline is Comparison B BY SUBSTRATE: SUSHI distribution shape carries +0.05–0.09 MRR over the length floor (4–7× chance), TRUCE is indistinguishable from the length floor — the pooled residual is a verified mixture, never quoted pooled. znorm float32-constant finding: committed baselines embed the constant signal as a ±1 row (znorm not to be changed). Probe 3 remains open for floor, TRACE, ChatTS) — previously (rev. 13 — TRACE Probe-2 arm EXECUTED AND SCORED: **P2-9 CONFIRMED** (sf_all rel. degradation 97.8% ± 0.1% vs the ±0.05 pinned margin, all seeds); the pooled perturbation-profile ordering exposed as a MIXTURE ARTIFACT by the pre-registered strata check (S2 MISSED: masking/ex_half swap places between the V=168 and V=180 strata in all seeds) — sf_all is the only stratum-invariant condition; duration confound closed by data (structure kind, not span or length, drives the ex_half split; D2 HIT at 14.0 vs 15, borderline stated); RESIDUAL finding: 2.9–3.1× chance MRR survives sf_all, CI excludes chance everywhere — distributional information, measured hand-off to Probe 3; masking-dose sweep DROPPED with rationale; P2-6/P2-7 verdicts lifted from the log into §2. Probe 2 now complete on CLaSP + floor + TRACE; ChatTS only remains) — previously (rev. 12 — FLOOR Probe-2 arm EXECUTED AND SCORED: **P2-4 CONFIRMED** (24/24 TOSTs, max inference |Δ| 0.0105; scoring pinned pre-run); D2-F amendment (identity-level ties, floor arm) after the BLAS column-path 1-ulp diagnosis; named finding: floor DiD mimics CLaSP's P2-2 surface signature with opposite (invariant-driven) composition — decomposition is the discriminator; TRACE runner next)
**Supersedes:** `thesis_state_document_final2.md` (v2), which is superseded in §2 (status), §4 (probe metric decision), §5 (baseline values), §6 (new verified facts), §7 (compute) and §8 (phase status). **If v2 and v3 conflict, v3 is correct.**

---

## 0. How to use this document (instructions for future sessions)

1. This is the authoritative project state. Read it before proposing anything.
2. **Companion documents in the repository** (all authoritative within their scope):
   - `docs/REIMPLEMENTATION_SPEC.md` — CLaSP architecture per paper vs. our documented choices. Do not silently change an "OUR CHOICE" default.
   - `docs/clasp_reimplementation_validation.md` — the fidelity argument for the reimplementation (thesis + defense material).
   - `docs/finding_metric_saturation.md` — the metric-saturation finding (thesis motivation + methodology justification).
   - `docs/probe1_findings_clasp.md` — Probe 1 on CLaSP: results, interpretation, threats to validity, open items.
   - `docs/probe1_findings_embedding_floor.md` — Probe 1 on text-embedding-3-large: the floor baseline, its VOID verdict, and the item-set length audit.
   - `docs/probe1_per_pair_cross_analysis.md` — per-pair CLaSP-vs-features analysis (closes findings §7 item 1; §10 adds the 279-restriction sensitivity, closing item 3).
   - `docs/probe1_manual_validation_findings.md` — the full item-validation arc: 50-item sample, mechanical audits, complete 863-item C4 census, cleaned headline (closes findings §7 item 2).
   - `docs/pinning_spotcheck_judging_rules.md` — the census judging protocol (rules R1–R5, conventions, criterion).
   - `docs/project_log.md` — chronological record of what was done and when.
   - `docs/correction_and_hardening_sheet.md`, `docs/revised_sections_paste_pack.md` — proposal correction record (applied).
3. **Provenance warning:** the archival files (`论文解读.docx`, `论文解读.xlsx`, `history_conversation_with_claude4_7.docx`, `在formulate_proposal过程中的一些思路和reference.docx`) contain known drift errors. They are kept as historical record only. Never import facts from them without checking §6 below.
4. **Honesty norms this user holds:** distinguish explicitly between (a) content read fresh from source in the current context, (b) content carried via summary/memory, and (c) inference. When asked "have you read X?", answer literally and offer to actually read rather than reassure. Never claim a fetch, read, or check that did not happen.
5. The goal is **clean graduation, not publication**. Keep scope tight; resist scope creep.

---

## 1. One-sentence description

A diagnostic evaluation framework that audits **representative** TS–text alignment models (CLaSP, TRACE, ChatTS, text-embedding-3-large) through three controlled probes — compositional component-swap, order-invariance via shuffling, summary-statistics sufficiency — to attribute aggregate retrieval performance and expose reliance on identifiable statistical shortcuts: **testing for non-shortcut alignment behavior, not certifying understanding**.

**Main RQ:** When TS–text alignment methods report high cross-modal retrieval accuracy, to what extent does this performance survive controlled probes that rule out compositional shortcuts, order-invariant matching, and summary-statistics matching?

**Scope of claims:** the probes detect the presence/absence of specific shortcuts. Passing all three = alignment not reducible to the three tested shortcuts. Failing = positive demonstration of shortcut reliance on the affected subset. "Genuine understanding" may appear only as the thing that *cannot* be certified.

---

## 2. Current status (2026-08-08)

**Paperwork — complete.** Proposal revised (19 edits applied: citation corrections, scope-of-claims reframe, statistical-analysis subsection, compute plan, Probe-1 protocol) and sent to the supervisor. Supervisor's four feedback points all addressed (§3).

**Phase 1a (CLaSP) — complete for one seed, validated.**

| Item | Result |
|---|---|
| Unified corpus | 8,780 pairs; all counts verified against source releases |
| CLaSP reimplementation | built from paper spec; smoke test at ln(N) |
| Baseline training | Colab T4, 21 epochs, early stop at epoch 11 |
| Seeds | 42/43/44; early stop at epochs 21/26/24; best val loss 3.203/3.187/3.254 |
| **Frozen baseline** (mean ± SD, strict, pool 386) | **R@1 0.049 ± 0.006 · R@5 0.221 ± 0.010 · R@10 0.331 ± 0.012 · MRR 0.141 ± 0.008** |
| Strict by source | TRUCE MRR 0.105 ± 0.004; SUSHI MRR 0.328 ± 0.035, R@10 0.790 ± 0.008 |
| Soft mAP@10 (SBERT ts=0.5) | TRUCE 0.448 ± 0.020 — **paper's 0.458 lies inside the seed range (0.433–0.470)**; SUSHI 0.853 ± 0.016 |
| Fidelity across 4 protocols | TRUCE max deviation 0.059 = 10–52% of each protocol's informative range; qualitative pattern reproduced |
| Negative control (untrained) | at chance on strict retrieval (no leakage); under the four protocols scores 0.246 / 0.015 / **0.999** / 0.326 |
| Canonical file | `results/experiments/baseline_clasp.json` |

**Open logistics:** GPU access for ChatTS — supervisor asked, no reply yet; Colab Pro purchased and sufficient for CLaSP-scale work, but **not** for ChatTS (needs guaranteed A100 → rented pod). Author email to Hitachi (CLaSP code / SUSHI Base / which SUSHI version) sent, no reply yet.

**Probe 1 (component swap) — built, and run on CLaSP only.** Component grammar derived and validated from SUSHI labels; 5,540 forced-choice items over 279 held-out signals; evaluated on all three checkpoints; difficulty control and statistics complete. See `docs/probe1_findings_clasp.md`.

| component | acc random | acc swap | gap (mean ± sd over seeds) |
|---|---|---|---|
| C5 signal regime | 0.953 | 0.984 | −0.031 ± 0.006 |
| C2 trend family | 0.987 | 0.951 | +0.036 ± 0.007 |
| C1 trend direction | 0.985 | 0.911 | +0.075 ± 0.017 |
| C3 periodic waveform | 0.925 | 0.743 | +0.182 ± 0.006 |
| C4 fluctuation type | 0.969 | **0.599** | **+0.371 ± 0.007** |

Chance = 0.500. All Holm-significant in all three seeds. Difficulty control: 16 hand-written features separate the same pairs at 0.919–0.988 across *all* components (SD 0.030) while CLaSP spans 0.599–0.984 (SD 0.164) — the flat baseline is what closes the "this distinction is just hard" objection. Headline: **global shape is encoded, local fluctuation texture is not.**

**Probe 1 hardening (2026-08-02 → 08-06) — complete.** All three strengthening items from `probe1_findings_clasp.md` §7 closed; details in the two new findings docs (§0.2):

1. **Per-pair cross-analysis** (item 1): CLaSP's 19 failing pairs (<0.70) have *median feature accuracy 0.950*; within-component correlations null in C3/C4; the only feature-hard pair (`sinusoidal|triangle`, 0.507) is one CLaSP beats (0.655). Refinements: C1's degradation is entirely one collapsed pair (`rev-sawtooth|sawtooth` 0.440, with a replicated directional inversion — model prefers "reverse sawtooth" captions for true sawtooth signals); C3's degradation concentrates in ramp-orientation confusions while square-wave pairs are intact. Blind-spot outcome, as pre-registered.
2. **279-restricted feature control** (item 3): identical protocol re-scored on exactly the 279 probe signals, gated on exact reproduction of all 124 committed accuracies (passed); deltas within sampling noise; re-running the per-pair analysis under restricted accuracies changes no conclusion. The population asymmetry is retired.
3. **Item-validation arc** (item 2): automated structural gates 2,770/2,770; 50-item human sample 46/50 (strict plain-language convention; 48/50 under corpus semantics, both disclosed); mechanical audit of all 990 C4 items; then a **complete human census of the 863 lexically-explicit C4 items: 738 valid (85.5%)**, five defect mechanisms fully characterised (subset 66, non-pervasive noise 42, bare clauses 3, "Large part," truncation 10, reverse overlap 4), zero mixed verdicts across repeated clauses. Re-grading CLaSP's stored answers on the certified items: **C4 = 0.603 [0.567, 0.641] (signal-level bootstrap)** vs features 0.929/0.931 and CLaSP's own 0.969 on random distractors over the same signals. **0.603 is the C4 headline; 0.599 is retained as the all-items figure.** Durable caveats: pn-spike pairs footnoted in both directions; random-condition distractors not human-validated; registered predictions missed twice and recorded as misses.

**Probe 1 on text-embedding-3-large (floor baseline) — complete.** Same items, same statistics; serialisation documented (z-normalised, ×10, clipped ±99, all 2,048 points, 4,096 tokens/signal). Result: **at or below chance on every component**, swap margins 0.001–0.007 vs CLaSP's 0.02–0.50. Choices correlate with caption length (r ≈ +0.13/+0.17), falling below chance where correct captions are shorter.

**Strict retrieval baseline for text-embedding-3-large — complete 2026-08-09** (`models/openai_embed/run_baseline.py`; protocol identical to CLaSP harness B, serialisation imported from the probe runner, shared cache; all registered count expectations hit exactly). Result over 878 test queries, pool 386: **all MRR 0.027 / R@10 0.052** (chance references 0.017 / 0.026); truce MRR 0.032; **sushi MRR 0.004, median rank 307, zero top-10 hits — below chance.** This anchors the probe's VOID verdict in ordinary retrieval units: the model has no retrieval capability for a perturbation to degrade. The below-chance SUSHI pattern is consistent with the probe's documented length-correlated behaviour; the mixed-pool crowding mechanism (short TRUCE strings outranking 4,096-token SUSHI serialisations, predicted median ~316 vs observed 307) is recorded as **inference, not verified** — accepted-and-footnoted by decision 2026-08-09, no diagnostic script. Canonical file: `results/experiments/baseline_openai_embed.json`.

**Its verdict is VOID, not "degraded".** With both conditions near chance there is no capability for a perturbation to degrade, so its gaps are *not* shortcut evidence. It contributes (a) a measured floor showing CLaSP's shape performance is bought by contrastive training, and (b) a negative control demonstrating the diagnostic does not manufacture false shortcut claims — a shortcut requires *high* random-condition accuracy, which this model never reaches.

**Item-set audit (applies to all models).** `scripts/audit_item_balance.py` reports the accuracy of an oracle that always picks the longer caption: **all swap conditions within 0.017 of chance** (0.495–0.517), so length is unexploitable where every shortcut finding comes from. Random condition deviates mildly (0.421 on C3 to 0.559 on C4) — report alongside random accuracies; it cannot account for CLaSP's 0.92–0.99.

**TRACE task zero (2026-08-07/08) — complete; checkpoint verified alive.** Stored args read from the released checkpoint (all gates passed): text encoder **nomic-ai/nomic-embed-text-v1.5**, **hard-negative mining ON (32 negatives)** — the scientific premise of the arm holds; 11,551,959 params reconciling with file size; `seq_len_channel = 186` (new constraint, see next steps); no training bookkeeping stored. Demo reproduction on the released test split (n=2006, full pool): **P@1 0.4167 text→ts / 0.4282 ts→text, median rank 2, MRR ≈ 0.55**, vs paper's 44.10% — within ~2 points, but the published number's split/direction/pool are unpinned, so this is orientation, not exact reproduction. Five defects catalogued in the published TRACE artifacts (authors' demo crashes as published; Stage-1 checkpoint never released; stored model name unimplemented in public code and architecture-changing; README data layout differs from what the code reads); all caught by gates; the CATSEncoder→TraceEncoder rename hypothesis confirmed by strict state-dict load with zero mismatches. Scripts: `models/trace/read_checkpoint_args.py`, `models/trace/run_authors_demo_eval.py`; canonical result `results/experiments/trace_demo_repro_test.json`; handoff §4.0 has the full record. Registered-prediction miss recorded: num_negatives 32, matching neither the yaml's 64 nor the CLI default 10.

**TRACE substrate decision (2026-08-08) — RESOLVED: option (b), narrative-level probe on NOAA.** The pre-registered downsampling gate FAILED: C4 feature separability drops 0.929 → 0.773 at 186 points, with all spike-polarity pairs collapsing to near-chance (neg-vs-pos 0.525) while global-shape components are untouched (0.93–0.99); robust across interpolation/decimation/window variants (`results/analysis/trace_downsample_survival.{json,png}`). Option (a) is therefore dead for C4 — substrate loss and model blindness would be confounded. Option (b)'s viability gate is passed by construction (demo repro = TRACE alive on this substrate at P@1 0.42). The narrative grammar (N1 labels antonym, N2 temporal extent, N3 trend direction, N5 location negative-control) was designed from the serializer read from source; **N4 (fluctuation↔stability) was dropped under a pre-committed rule** — only 33/2,006 rows swap cleanly once evidence clauses are blocked, so with the downsample FAIL the C4 question cannot be posed to TRACE in either direction (two-walled finding; supervisor talking point). The item set is **CERTIFIED 2026-08-09** after the full arc (round-1 50-item judgment → rule fixes incl. the N1 post-swap-contradiction gate that removed 30% of the pool → v2 regeneration → round-2 20-item judgment → population audits (N1 0/400 contradictions; N3 peak-family census) → human certification of 15 suspects, 11 excised with twins): **`data/processed/narrative_probe_items_certified.jsonl`, 3,178 items (N1/N2/N5 400+400; N3 389+389)**, excision record kept, both counts reported as with the C4 census. Two registered corrections: (i) the matrix line below on "human narratives" is unimplementable as stated — the retrieved description is largely LLM-generated channel prose (human text = event narratives, 659/2,006 rows, separate signal-side stream); documented limitation, raise with supervisor. (ii) Text-overlap validity threat registered — and REVISED 2026-08-09 from source: no inference-time text-matching pathway exists (the retrieval score uses a signal-only embedding computed before cross-attention; `mm_encoder.py` read with line numbers in handoff §4.1); only a training-mediated cue-learning version survives, which is what the probes test anyway. Header-vs-prose downgraded to descriptive; the observed direction (prose MOST degraded) is the opposite of contamination. Full record: handoff §4.1 and §4.5.

**TRACE narrative probe (2026-08-09) — MEASUREMENT COMPLETE AND REPLICATED; ~~HARDENING OPEN~~ HARDENING CLOSED same day (N3 census verdict, item 1 below; header left stale — corrected 2026-08-16 in the full-docs drift audit).** Runner built from source reads (description path verified independent, G7 diff 4.5e-07; authors' eval uses a RANDOM 30% mask — replicated over seeded draws 13/14/15, unperturbed P@1 0.428–0.441, spread 0.0125). All gates green after one caught bug (error #10). Results (3,178 items × 3 seeds; no VOID; random condition 0.990–1.000): swap accuracy / gap — N1 0.875–0.892 / +0.11; N2 0.935–0.950 / +0.055; **N3 0.720–0.725 / +0.27 (largest gap; margins 0.005; internal duration gradient week 0.648 → six-months 0.841 must be reported with any N3 claim)**; N5 0.917–0.935 / +0.074. All Holm-significant, all seeds. TRACE is not CLaSP: partial degradation everywhere, no collapse — hard negatives help but do not eliminate component-swap sensitivity. **N5 reframed:** the designed negative control detects location at 0.92 — and at **0.900 on the 40 place-name-only swaps** (frame confound ruled out; length ruled out) — so location IS signal-inferable to TRACE; climate-inference vs station-memorization left open; not reportable as a negative control; supervisor item. Length confound closed (N1/N3 swap sets zero-word-diff by construction; flagged cells deflationary). Prediction ledger: 6 confirmed, 2 missed, 1 partial — misses recorded (handoff §4.5). **Hardening CLOSED 2026-08-09 — N3 CENSUS-CERTIFIED; TRACE Probe-1 arm COMPLETE.** The screen failed (primary 86/100; two-pass union 17 defects), rule-certification was refuted by counterexample (N3|1577), and Ranyi then judged ALL 289 remaining flagged items — since 389/389 were flagged, this makes a true full census of N3. Result 261 y / 28 n (census rate 9.7%; whole-population 45/389 = 11.6%; six_months stratum worst at 14/23 = 60.9% but not structural). 45 defective swap items excised with matched random twins → 344 certified pairs. **Certified quotable headline: N3 swap 0.703–0.712, gap +0.289 ± 0.007, Holm-significant all seeds, random 0.994–1.000.** Swap accuracy DECREASED in every seed after excision (Δ up to −0.016) — P-cen3 confirmed in the informative direction, demonstrating the pre-census +0.27 was understated. Certified gradient monotone: week 0.619 (n=90) → 28_days 0.736 (n=245) → six_months 0.852 (n=9; too thin for standalone claims). Predictions: P-cen1 confirmed (28 ∈ [25,75]), **P-cen2 missed** (six_months 9/17 = 0.529 vs ≥0.70 — R1 over-strong), P-cen3 confirmed. Count chain to report: 400 → 389 → 344. Canonical records: `results/experiments/trace_narrative_{per_item.jsonl,summary.json,statistics.json}` (pre-census, retained), `results/experiments/trace_narrative_per_item_certified.jsonl`, `results/experiments/trace_narrative_statistics_certified.json`, `results/analysis/{n3_census_sheet.csv (filled), n3_census_excision_ids.txt, n3_census_verdict.json, trace_narrative_slices.json, probe1_item_balance_trace_narrative.json}`. Remaining optional extras (unscheduled): N5 interpretation deep-dive; restricted option-(a) garnish.

**Not started:** Probe 1 on TRUCE substrate (optional). **ChatTS: ARM CLOSED 2026-08-16 — all three probes executed, scored, and recorded (results record below; handoff §4.9).** **Probe 3: ALL FOUR ARMS COMPLETE** (CLaSP/floor/TRACE 2026-08-15/16; ChatTS 2026-08-16 — no shortcut). **Probe-2 EXECUTION COMPLETE on all four models.**

**Immediate next steps, in order:**
1. ~~N3 census verdict~~ **DONE 2026-08-09** — N3 census-certified (344 pairs; headline swap 0.703–0.712, gap +0.289 ± 0.007); TRACE Probe-1 arm complete. **Now first: supervisor conversation** (N5 reframing, threat revision, two-walled C4, certified N3 headline + gradient with the thin six_months cell, understatement demonstrated by post-excision decrease, sample-to-screen/census-to-certify arc now demonstrated twice).
2. **ChatTS** (blocked on GPU access) — MCQ reformulation.
3. TRUCE substrate with parse-coverage reported (the only remaining §7 strengthening item; optional).
4. **Probe-2 execution status: COMPLETE on three of four models (ChatTS remains, GPU session).** CLaSP arm DONE AND SCORED (2026-08-13): P2-1 CONFIRMED (SUSHI dep sf-all Δ 0.232/0.239/0.280, rel 76–78%, Wilcoxon p ≤ 4e-16), P2-2 CONFIRMED (TRUCE DiD +0.0022/+0.0762/+0.0729; 95% CIs exclude 0 in seeds 43/44), P2-3 CONFIRMED, P2-5 MISSED (TOST ±0.05 pinned pre-analysis: PASS in seeds 43/44, seed 42 fails via ONE query — 'The majority is flat.' → rank 1→259; registered interpretation instantiated). **Floor arm DONE AND SCORED 2026-08-14: P2-4 CONFIRMED** (runner v2 with D2-F; 24/24 TOSTs; max |Δ| 0.0105; $0.75 spent); named finding: floor TRUCE DiD mimics P2-2's surface signature with invariant-driven composition — decomposition is the discriminator. **TRACE arm DONE AND SCORED 2026-08-15: P2-9 CONFIRMED** (sf_all relative MRR degradation 97.8% ± 0.1%, dep n=2,005, text→ts, vs the ±0.05 relative margin pinned pre-analysis; all gates green, G6 digit-exact 884/859/863, zero exact ties any seed/direction). Grouping-census scorecard entries previously recorded only in the log, now stated here: **P2-6 CONFIRMED** (SUSHI invariant cell 4 ≤ 14; within-SUSHI DiD underpowered as suspected) and **P2-7 CONFIRMED** (TRACE invariant captions 0% — the DiD-unposable finding). Full Probe-2 ledger: P2-1/2/3/4/6/7/9 CONFIRMED, P2-5/8 MISSED with mechanisms recorded.

**TRACE Probe-2 results record (2026-08-15; canonical files `results/experiments/probe2_trace_{summary,stats}.json`, strata/duration in `results/analysis/`):**
- **The pooled profile ordering is a MIXTURE ARTIFACT — report by stratum, never pooled.** Pooled (dep n=2,005, unperturbed MRR 0.5563 ± 0.0065): sf_all 97.8±0.1% > sf_half 92.7±0.3% > masking 64.7±0.6% > ex_half 56.1±0.6%. The pre-registered strata check (S2 MISSED) shows the last two SWAP PLACES between strata in all three seeds: V=168 rows (week/28-day, n=1,050) masking 70.1% > ex_half 37.8%; V=180 rows (six-month, n=544) ex_half 81.9% > masking 59.3%; between-stratum ex_half difference −44 points, CI excludes 0; Spearman(V, log rank ratio) ρ≈+0.27, p≈1e-35. The pooled 56.1% describes no population. This also explains the pre-flagged fragile ex_half-vs-masking contrast (56.6–59.1% per-query dominance): two strata with opposite orderings, larger stratum outvoting.
- **sf_all is the only stratum-invariant condition** (97–98% in every stratum and duration cell; between-stratum CI includes 0 in all seeds) — destroying local order is catastrophic for TRACE regardless of signal type. All Wilcoxon p_holm ≤ 1e-138.
- **Duration confound closed by data** (labels from the committed narrative records, 1,203/2,006 rows; V=180 = six_months 278/278 pure; V=168 = week 364 + 28_days 323): ex_half degradation week 47.1% / 28_days 33.0% / six_months 84.2%. Registered D2 scored HIT at 14.0 vs the <15-point threshold — **borderline; per the standing rule the substantive claim is stated separately**: the within-length week-vs-28_days difference is real (CI excludes 0 all seeds, +12 to +17 points) but ~3× smaller than the 44-point cross-structure gap. Quotable form: **series length is excluded, span has a real but modest effect, and the dominant driver of the half-swap's damage is structure kind — it inverts a seasonal arc but largely spares repeating diurnal cycles.** Unregistered observations, recorded as such: masking also splits week from 28_days (+14..+22, CI excl. 0); 28_days is the easiest cell to retrieve unperturbed (0.72–0.74 vs week 0.43–0.45, six_months 0.58–0.62), an ordering that does NOT match the narrative arm's duration gradient (S3 MISSED — gradients are probe-specific).
- **RESIDUAL ABOVE CHANCE: sf_all retains 2.9–3.1× chance MRR** (median rank ~620 vs chance 1003), CI excludes chance in every seed and direction. Shuffling preserves each channel's value multiset exactly, so the residual is distributional, not sequential — **a measured hand-off to Probe 3**.
- Caveats standing with the arm: no invariant group ⇒ no DiD and no TOST (design fact per the accepted reframe); shuffle/mask doses are not matched (100% of order vs 20% of values) so no claim of the form "order matters more than values" is made anywhere — the planned dose sweep was DROPPED 2026-08-15 because the pooled ranking it would have calibrated does not exist as a single fact; bootstrap resamples rows and cannot model pool-side dependence (pool = queries), stated, immaterial at these effect sizes; masking reported as 0.3 protocol + 0.2 input (effective 0.1976), never bare 0.2; ambiguous row 1191 (n=1) descriptive only. Mechanics T1–T13 accepted 2026-08-15; the two setup diagnostics' missed predictions (leading padding; instance normalisation) are the arm's foundation — both would have produced clean runs with wrong numbers. NEXT: ChatTS (GPU) or Probe 3 design.

**Probe-3 CLaSP results record (2026-08-15; canonical `results/experiments/probe3_clasp_{summary,stats}.json`):** conditions resample (i.i.d. with replacement from the series' own values) and matched gaussian, whole pool, raw-level, vs the frozen baselines; sf_all rung read from committed Probe-2 records (JG join digit-exact). **Quote by substrate only.** SUSHI: resample 3.6–6.3× chance (dependent cells), gaussian at/below global chance; resample−gaussian gap +0.062/+0.052/+0.095 MRR, CIs exclude 0 — value-distribution SHAPE carries real retrieval. TRUCE: gaussian sits at the LENGTH-SPLIT reference (0.0239/0.0232/0.0252 vs 0.0247; frac(rank≤246) 0.93–0.95; median 117–122 vs 123.5 uniform-in-block) and resample adds nothing the CIs can see. Ladder placement: CLaSP-SUSHI's order-free residual is distributional; CLaSP-TRUCE's is length matching. P3-2 misses: TRUCE resample degrades MORE than shuffle (direction all seeds; the pre-registered 12-point coarseness mechanism, G12-evidenced); SUSHI equivalence unresolved (42/43 margin unreachable at n=135 — recorded oversight; 44 reversed significantly — flagged anomaly, TRACE arbitrates at n=2,005). P3-5 (spike vs clean/smooth) MISSED under the pinned direction-rule; medians favor spikes in all seeds and step's retention is high (0.35–0.78) — both unregistered observations, so WHICH distributional feature carries the SUSHI signal is open (tails vs bimodality). Caveats standing: gaussian = length-only floor, not absolute chance; SUSHI under-noise length behavior open; seed-44 anomaly open; TOST legs quoted with their n.

**Probe-3 FLOOR results record (2026-08-15; canonical `results/experiments/probe3_openai_{summary,stats}.json`; per-query records arms 42/43/44):** runner `models/openai_embed/run_probe3.py` (v2), stats `models/openai_embed/analyze_probe3.py`; conditions resample + gaussian, raw-level then serialize() (the floor-native float64 path — deliberate, documented against the error-#14 dtype confusion; SC-1 retired, one serialisation path); D2-F on; sf_all rung read from the committed floor Probe-2 records (JG digit-exact, pre-verified). **P3-7 CONFIRMED** under the pre-pinned P2-4-pattern scoring: 12/12 TOSTs (2 inference cells × 2 conditions × 3 arms; ±0.05 ABSOLUTE MRR; 90% cluster-bootstrap CI, signals, B=2000), max inference-cell |Δ| 0.0067 (truce_dep/resample, arm 42 — the registered value exactly); verdict VOID as pre-declared — the negative control shows the ladder machinery does not manufacture degradation. REF cross-computation gate (independent Claude-side table from the uploaded records, disclosed) reproduced 4/4 deltas ≤1e-9 in every arm. Cost: $0.45 once; the G8-rank stop cost $0 (vectors cached pre-stop) and the rerun's cache determinism is proven by the exact 298→273 reproduction. Dry-run ledger: point predictions HIT (2,310 new strings exactly; $0.45; ties 24/0; float-level 22; no-ops 1 × 6 sets); two expectation-LABEL misses closed by arithmetic (TRUCE unique quantised tokens 6.54/12 vs the raw-value 9.63 — apples-to-oranges; 9.63 × 0.704 survival = 6.78, minus quantisation merges; TRUCE frac-unique-missing 0.2959 = the CLaSP-arm corrected ~0.29 — the 0.35–0.37 band belongs to positions-never-drawn). Report-only observations: thin-cell DEFLATION (see header) and a floor-scale ladder glimpse — on the thin TRUCE cells sf_all roughly preserves the lucky unperturbed MRR while resample/gaussian destroy it (token-overlap matches survive reordering, not value replacement; descriptive, n=5/18, mechanism hypothesis only). Errors: #15 (G8-rank, §2b) plus the G-cost $0-rerun refusal caught by PREDICTION before the rerun, not by a crash.

**Probe-3 TRACE results record (2026-08-16; canonical `results/experiments/probe3_trace_{per_query_seed13/14/15.jsonl, signal_meta_seed13/14/15.json, summary.json, stats.json}`, setup diagnostic `results/analysis/probe3_trace_setup_diagnostic.json`):** runner `models/trace/run_probe3.py`, diagnostic `models/trace/diagnose_probe3_setup.py`, stats `scripts/analyze_probe3_trace.py`. Conditions resample (JOINT index draw across all 7 channels — preserves cross-channel co-occurrence per the joint-shuffle precedent) and per-channel matched gaussian, both valid-block-only [186−V,186) then per-channel RENORMALISATION (ddof=0, measured from data AND confirmed from source — the loader's StandardScaler; stats float64, tensor float32). **Renorm-always is exact raw-level reconstruction**: resample/gaussian commute with per-channel affine maps once renormalised (measured ≤1e-6), so the normalisation locus (loader-side, a registered-miss reading) is immaterial by construction. Dead channels (498 = 441 all-zero + 57 float-dust, predicted exactly) pass through bit-identically (G8-T ×2 conditions ×3 seeds). Constant-collapse redraw rule: row-level joint redraw, seed suffix `|redraw{k}`, max 8; census 41/25/42 for seeds 13/14/15, deterministic, HARD-gated diagnostic↔runner and reproduced exactly; collapse mechanism CONFIRMED in the general form (unique-value counts 2–4 in every case) after the registered nonzero-count operationalisation missed (dominant repeated value need not be zero). sf_all rung READ from committed Probe-2 records; pairing proven in-runner (R-JG: unperturbed D2 ranks identical to committed, ≤1e-9, both directions, all seeds) and re-verified at stats time. G6 frozen 884/859/863 digit-exact; ties zero except ONE gaussian text2ts query in seed 15 (recorded miss, absorbed by D2). **Ladder (dep n=2,005, text→ts, mean±sd):** unperturbed 0.5563 ± 0.0065 → sf_all 0.0124 ± 0.0005 (3.05×) → resample 0.0111 ± 0.0012 (2.71×) → gaussian 0.0055 ± 0.0003 (1.36×); resample-vs-gaussian and sf_all-vs-gaussian Wilcoxon p_holm ≤1e-19 everywhere, dominance ~59–60%. Chance references from MEASURED sizes: global 0.0040784; ceilings V=168 (n=1,051) 0.0071695, V=180 (n=544) 0.0126417, other composite 0.2658. Caveats standing: gaussian numbers quoted WITH stratum position (anchor-conditional discipline retained though the anchor behaved as designed here); the small gaussian residual >1× (significant in seed 14 only) has ONE unregistered mechanism hypothesis on record — dead-channel fingerprints survive both surrogates by design — checkable, not claimed; ts2text seed 14 shows resample significantly MORE degrading (95% CI [+0.0006,+0.0067]), secondary direction, sub-margin, stated; ambiguous row 1191 hit rank 1 under resample in all seeds text→ts (sf_all 5/1/1, gaussian 4/6/62) — census profile shows zero order-dependent hits and eight variability/stability hits, mechanism hypothesis: pure-variability caption survives pool-wide structure destruction; n=1, never quotable. Probe-3 ledger after this arm: P3-2a/3/4/7 CONFIRMED; P3-1/2b/2c/5/6 MISSED with mechanisms recorded; P3-2b seed-44 anomaly ARBITRATED (equivalence at n=2,005).

**ChatTS results record (2026-08-16; canonical `results/experiments/chatts_{probe1,probe2,probe3}_responses.jsonl` + `chatts_analysis.json` + `chatts_probe3_contrasts.json`; pod logs `results/logs/chatts_gpu_session/`; scripts `models/chatts/run_probes_gpu.py`, `scripts/analyze_chatts_probes.py`, `scripts/analyze_chatts_probe3_contrasts.py`):** metric = MCQ accuracy, both orders averaged per unit (0/0.5/1); signal-cluster bootstrap B=2000 (95% cells, 90% TOST); TOST ±0.05 on ACCURACY = a flagged new application of the pinned MRR margin; Wilcoxon on order-mean diffs primary + Holm across components, strict-binarisation McNemar secondary; P(A) and order-flip rate as standing diagnostics.
- **Viability:** SUSHI random-condition 0.726 [0.695, 0.755] — capability present, below the registered 0.90 (PC1-1 MISS; miscalibrated on paper-reported capability). **TRUCE 0.622 [0.591, 0.654] ruled WEAK-VIABLE** (the 0.60 convention line missed by 0.009 while chance is decisively excluded; argued per the never-threshold-alone rule; 12-point caveat and limited degradation headroom stated with every TRUCE cell). C3 (periodic waveform) stratum is VOID (random 0.570, CI lower 0.502).
- **Probe 1 (the NON-prediction answered):** C2 trend-family swap 0.527 [0.471, 0.576] — the CI contains chance — vs random 0.852; gap +0.326, Holm 5.6e-30, McNemar 179/22: **collapse.** C4 fluctuation 0.658 vs 0.755, gap +0.097, Holm 1.3e-9: **partial, no collapse** (contrast CLaSP's C4 blind spot; consistent with ChatTS's local-fluctuation training — interpretation, flagged). C1 and C5: no significant gap. Order-flip rates 0.28/0.23 (both-orders design absorbing real position sensitivity; pooled P(A) 0.442, per-probe 0.556/0.398/0.363).
- **Probe 2:** SUSHI dependent sf_all −0.267 [−0.348, −0.182] (0.733 → 0.467, below chance); **two-level shuffle: within-patch −0.019 TOST-flat vs across-patch −0.219 [−0.296, −0.141] — ChatTS reads order at the patch-arrangement level and largely discards it inside a 16-point patch** (patch-16 architecture visible in behaviour). TRUCE dependent: ex_half worst −0.174 [−0.215, −0.135], sf_all −0.113 [−0.149, −0.075], masking −0.018 TOST-flat and **PJ-defended** (PJ flat both substrates: TRUCE +0.000 ci90 [−0.025, +0.021], SUSHI −0.005 ci90 [−0.015, 0.000] — the load-bearing TRUCE-masking control passed by narrow CI; the frozen-prefix rerun decision did not fire; ChatTS measurably ignores its own prefix digits at masking magnitudes). **DiD with mandatory decomposition:** TRUCE sf_all +0.335 [0.143, 0.550] — over half carried by the thin invariant leg improving (+0.222 [0.053, 0.429], n=18); the dependent drop −0.113 is the load-bearing signal (the floor-mimicry lesson operating); sf_half +0.175 [0.027, 0.353]; ex_half +0.230 [0.002, 0.478]; masking +0.046 n.s. SUSHI sf_all +0.267 [0.186, 0.351], cleanly dependent-driven (invariant flat at 1.0, n=4 descriptive). Degenerate constant: correct and letter-identical in all 8 conditions — identity and determinism demonstrated at results level.
- **Probe 3 — NO SHORTCUT:** rung 1 stands alone (0.743 [0.682, 0.807] SUSHI / 0.622 [0.590, 0.653] TRUCE); every sub-order rung's CI contains chance on both substrates (rung 2 sf_all 0.486/0.516, five-number 0.482/0.519, resample 0.471/0.512, gaussian 0.464/0.501, cond_A, cond_C). Five-number vs rung 1: −0.261 [−0.343, −0.186] / −0.103 [−0.142, −0.066] — an explicit statistical summary buys nothing. **cond_B ≈ rung 1 flat both substrates — prefix content is inert with the series present** (coherent with A≈C 0.982/0.974 flat, the registered near-construction, and the task-zero 2-field-prefix limitation). **Anchor: cond_C = stock gaussian EXACT (diff +0.0000, letter agreement 1.0000, both substrates) — the manual encoding path is proven at the results level.** Rung-pair contrasts: TRUCE all four (rung2↔resample, resample↔gaussian, rung2↔gaussian, fivenum↔rung2) certify equivalence at ±0.05; SUSHI rung2↔resample and resample↔gaussian certify, rung2↔gaussian (ci90 [−0.014, +0.057]) and fivenum↔rung2 (ci90 [−0.064, +0.054]) are **inconclusive-by-width at n=140** — CIs include 0, no measured difference anywhere; TOST non-transitivity stated. JG coherence: ladder rung 2 = the group-weighted Probe-2 sf_all cells digit-exact (0.4857/0.5163); rung 1 TRUCE = the viability number.
- **Thesis sentence:** ChatTS's retrieval competence is carried entirely by ordered structure — destroying order removes everything measurable and no lower information rung differs from any other — making it the first model in the matrix with **no Probe-3 shortcut**, alongside a **component-specific Probe-1 blind spot (trend family collapses to chance)** and a **patch-level order-reading signature** (Probe 2's two-level shuffle).
- Caveats standing with the arm: all ChatTS claims are conditional on WEAK-VIABLE TRUCE and the 0.73 SUSHI baseline (limited headroom stated); C3 VOID — no periodic-waveform read; thin cells (TRUCE invariant 18, SUSHI invariant 4, ambiguous 5, degenerate 1) always quoted with n; the SUSHI width-limited contrasts quoted as inconclusive, never as equivalence; readout is the documented MCQ adaptation (logit, both orders) — cross-model synthesis stays at relative degradation.

**Statistics script is now model-agnostic:** `--per-item` and `--out` flags; single-run inputs handled without implying replication; components with both conditions near chance labelled **VOID** rather than degraded.

The item set, difficulty control and statistics script are **model-independent** — each new model needs only an encode-signal / encode-text adapter.

---

## 3. Supervisor feedback (2026-07) and resolutions — all applied to the revised proposal

1. **Statistical significance** → new §6.6: paired bootstrap CIs on Δ, Wilcoxon signed-rank on per-query reciprocal ranks, McNemar for ChatTS MCQ, Holm–Bonferroni across Probe-1's five components, difference-in-differences for Probe 2, and **TOST equivalence testing** so that absence of degradation is a supported claim rather than a non-significant result.
2. **Cross-model applicability of the swap** → caption-side swap protocol; identical for CLaSP and text-embedding-3-large, MCQ reformulation for ChatTS, **reduced narrative-level variant for TRACE**; synthesis via relative degradation only.
3. **Platform and compute** → §7 below.
4. **BEDTime authors wrong; 0.59→0.24 sentence unclear** → authors corrected to Sen et al.; the figure re-attributed precisely to MMTS-Bench's own ChatTS-style reproduction (Qwen2.5-3B), with the OFF\* recovery to ≈0.60 added.

---

## 4. The three probes (final designs)

**Red thread:** never read absolute numbers; always relative degradation Δ = (baseline − probe)/baseline against each model's own unperturbed baseline.

**Equivalence margin (binding, ±0.05):** justified by the Phase-1a seed noise floor (R@10 3.5%, MRR 5.6%) recorded before any probe existed. It must be stated in the thesis that the margin was fixed *after* the gap point estimates were seen; it certified no component that would otherwise have failed, and CIs are reported throughout so readers may apply their own threshold. Do not revise the margin retrospectively.

**Probe-facing metric decision (binding):** all probe measurements use **strict pair-level retrieval** (Recall@k, MRR against the ground-truth pairing). The paper's soft judge-based mAP@10 is retained **only** for reproduction comparison. Rationale: the soft protocol saturates — one published configuration accepts 99.7% of all candidate pairs, and a randomly initialised model scores 0.999 under it — so it cannot register degradation, producing false negatives indistinguishable from genuine model robustness. See `docs/finding_metric_saturation.md`.

**Primary vs. secondary metric (binding, added 2026-07-27):** the three-seed baseline gives the seed-to-seed noise floor per metric — Recall@10 3.5%, Recall@5 4.4%, MRR 5.6%, **Recall@1 11.6% (and 32.0% on SUSHI alone)**. Recall@1 on SUSHI is dominated by small-count noise (140 queries, 15–26 hits). **MRR and Recall@10 are therefore the primary probe metrics; Recall@1 is reported but no conclusion rests on it.**

**Probe runs use all three seeds (binding, added 2026-07-27):** probe evaluation is inference-only and therefore nearly free to repeat, so every probe is run against all three checkpoints. Significance is established by *paired* tests within each seed (same query, perturbed vs. unperturbed — far lower variance than the seed spread), and replication across the three seeds is reported alongside. The seed noise floor is a conservative outer bound for interpreting effect sizes, not the significance test itself.

### Probe 1 — Compositional component-swap (parent: ARO)
- Five-component grammar: trend direction · trend type · seasonality period · anomaly presence · magnitude. Single-component, same-vocabulary, evaluation-time swaps.
- **Retrieval protocol:** fix the TS as query; candidate pool = correct caption + its single-component-swapped variants (+ optional random distractors to fixed pool size); report rank of the correct caption vs. a same-size random-distractor pool. All negatives are caption-side constructions — no swapped-attribute TS needs synthesising.
- **Primary substrate: SUSHI class labels.** Confirmed by inspection that SUSHI labels are already compositional (e.g. `negative spike; constant` = fluctuation component + trend component), and the paper documents three label categories — **Trend**, **Periodic**, **Fluctuation**. The component grammar can therefore be read from the labels rather than parsed from free text. 140 classes = combinations of these categories.
- **Secondary substrate: TRUCE** free-form captions — parser applied, parse-coverage rate reported, run only over the parseable subset, selection bias stated as a limitation, ~100 parses manually validated.
- Per model: CLaSP and text-embedding-3-large as-is; ChatTS as MCQ (F1 per swap type); TRACE reduced narrative-level components on NOAA.
- **Confound defense:** compositional sensitivity ⇒ *differential* degradation across components; a distractor-difficulty artifact ⇒ *uniform* degradation.

### Probe 2 — Order-invariance via shuffle (parent: Tan et al.)
- **Three** shuffles (sf-all, sf-half, ex-half) **plus** Tan's separate masking perturbation. (Not "four shuffle strategies.") Parent mechanics pinned from source 2026-08-09 — see §6.1 Tan row (test-time only; point-level; ex-half deterministic; masking→0; channels jointly).
- **Metric (BINDING, 2026-08-09): strict retrieval rank shift** — perturbed vs unperturbed rank of the ground-truth caption (MRR/R@10, primary per the standing rule), paired per query, reusing the frozen unperturbed baselines (CLaSP frozen table, TRACE seeded-mask demo repro, floor baseline). Forced choice was considered and **rejected**: Probe 1's justification (unequal component pools) does not transfer — Probe 2 swaps nothing caption-side — and a random-distractor forced choice starts at CLaSP's 0.95–0.99 ceiling, compressing the DiD twice. Per-group unperturbed baselines must be printed side by side before any DiD is quoted (groups may differ in baseline retrievability; DiD absorbs the level, but it is reported, not assumed away).
- **Floor model pre-declared VOID for Probe 2** (baseline MRR 0.027 vs chance 0.017 — no capability for a shuffle to degrade). It runs anyway as the pipeline's negative control: meaningful "degradation" reported for a model with no capability would indicate a broken pipeline, not a finding. **Scored 2026-08-14: P2-4 CONFIRMED** — no degradation beyond ±0.05 on any inference cell; the DiD-mimicry finding (see §2 and log) is the control's positive contribution: the DiD surface signature is reproducible from thin-cell noise, so decomposition is mandatory in every DiD report.
- **Grouping outcomes (certified 2026-08-10):** SUSHI 135 dependent / 4 invariant / 1 degenerate — within-SUSHI DiD underpowered; SUSHI = known-order-dependent sanity stratum + descriptive invariant mini-group + identity control. TRACE 2,005 dependent / 1 ambiguous (degenerate constant, row 1191) / **0 invariant** of 2,006 — **caption-group DiD unposable on TRACE; recorded finding** (benchmark text saturated with order language; 1,878/2,006 contain "trend"). TRACE's conclusion-carrier is the shuffle-vs-mask perturbation profile (PROJECT_CONTEXT). The CLaSP caption-group DiD lives on TRUCE — **TRUCE census-CERTIFIED 2026-08-13** (rules v2 with negation handling per the SUSHI negated-order-word precedent; full invariant census by Ranyi 184y/5n, dependent sample 30/30, ambiguous 40y/3n; GBK sheet round-trip caught with join keys verified intact): **dependent 7,089 rows / 4,862 texts / 715 test; invariant 245 / 185 / 18; ambiguous 46 / 40 / 5; degenerate 0** — canonical artifact `results/analysis/probe2_truce_groups_certified.json`. **P2-8 SCORED AND MISSED: 0.0334 vs the registered 0.15** (pinned pre-run: parseable = dep+inv, row-level population primary; provisionals v1 0.0328 / v2 0.0340 retained). The miss is itself the finding — three-substrate saturation: SUSHI 2.9% / TRACE 0% / TRUCE 3.3% invariant. Stated consequence: the DiD's invariant group on test is 18 rows, so the TOST leg (P2-5) is thin and is always quoted with its n.
- **Perturbation mechanics (accepted 2026-08-10, PROJECT_CONTEXT holds the full record):** SUSHI point-level; TRUCE sf-half = first 6 of 12, ex-half = swap 6-halves; TRACE joint-channel permutation, per-signal seeded; mask 0.2 (+0.3 standing protocol on TRACE, reported as such), fill 0. Registered predictions now P2-1…P2-9 (handoff §4.6).
- **Gap-2 refinement (the actual novelty):** split captions into order-dependent vs order-invariant groups; shuffle each; the diagnostic is the *differential* (difference-in-differences). Never revert to naive whole-set shuffling.
- Classifier validation: 3-way (clearly dependent / clearly invariant / ambiguous-excluded) + human check on a sample.
- ChatTS extra: two-level shuffle (within-patch vs across-patch).

### Probe 3 — Summary-statistics sufficiency (concept parent: Geirhos et al. 2020) — DESIGN ACCEPTED 2026-08-15 (full record: handoff §4.8)
- Information LADDER per model: original → sf_all (exact multiset; from committed Probe-2) → resample-with-replacement (distribution; load-bearing) → matched gaussian (length-only floor — all runnable models normalise away mean/std; amendment 2026-08-15). Constant rung dropped; five-number condition deferred to ChatTS (A/B/C, prompt substrate) as a documented adaptation. Paper-native precedent unchanged: MMTS ON 0.59 → OFF 0.24 → OFF\* ≈0.60; ChatTS RQ5 noise-attribute inversion.
- Raw-level surrogates, whole pool, frozen baselines, strict rank shift (extension of the Probe-2 binding metric), two numbers per cell (rel. degradation + ratio-to-chance), TOST ±0.05 for resample≈shuffle, ladder-profile verdicts, pre-registered strata (CLaSP per-source; TRACE V=168/180). Predictions P3-1…P3-7; gates G1–G12 (handoff §4.8).
- The z-normalisation note is now LOAD-BEARING and measured: the gaussian rung is chance-by-construction only up to LENGTH, which every surrogate preserves by design — reported as the length floor.
- Bonus (discussion-level, unchanged): TRACE channel-index probe with i.i.d. Gaussian replacement (not zeros).
- Status: CLaSP + floor + TRACE COMPLETE (see §2; P3-7 CONFIRMED — pipeline negative control green; P3-2a/P3-3 CONFIRMED 2026-08-16 — residual bridge closed, distribution shape not length; P3-6 missed mildly; seed-44 arbitrated); ChatTS GPU remaining. *(Line corrected 2026-08-16 — the rev. 16 paste pack omitted it; drift caught in Ranyi's §8 review.)*

---

## 5. Evaluation matrix

| Model | Type | Probe substrate | Probe metric | Status |
|---|---|---|---|---|
| **CLaSP** (our reimplementation; Ito et al., EUSIPCO 2025) | univariate dual encoder (Transformer-from-scratch + T5-Small) | TRUCE / SUSHI Tiny | Recall@k, MRR | **baseline done**: R@1 0.043, MRR 0.133 |
| **TRACE** (Chen et al., arXiv 2506.09114) | multivariate retriever, CIT + channel-biased attention, K=32, λ_ch=1.0 | NOAA test-split descriptions via the authors' generate_dsp template (largely LLM channel prose — the "human narratives" framing was corrected 2026-08-08, see §2; narrative grammar N1–N5) | binary forced choice, acc gap swap-vs-random | substrate resolved (b); 3,178 certified items (N1/N2/N3/N5; N4 unbuildable-clean, dropped); measurement complete 2026-08-09, replicated over 3 mask seeds; N5 reframed (not a negative control); **arm COMPLETE 2026-08-09 — N3 census-certified (344 pairs; gap +0.289 ± 0.007)** |
| **ChatTS** (Xie et al., VLDB 2025) | 14B Qwen2.5 TS-MLLM, value-preserved prefix (paper era: 2 fields — offset+scaling; fp16) | TRUCE / SUSHI (MCQ, logit readout, both answer orders) | accuracy; Wilcoxon+Holm primary, McNemar secondary | **ARM CLOSED 2026-08-16**: viable (SUSHI 0.726; TRUCE WEAK-VIABLE 0.622); C2 collapse / C4 partial; patch-level order reading; **no Probe-3 shortcut**; `chatts_analysis.json` |
| **text-embedding-3-large** | API text embedder | TRUCE / SUSHI (series serialised as text) | Recall@k, MRR | **baseline done 2026-08-09**: MRR 0.027 vs chance 0.017, median rank 133/386 — floor confirmed; SUSHI below chance (median 307), footnoted; `results/experiments/baseline_openai_embed.json` |

Cross-model synthesis uses **relative degradation only**. No shared test items across all four; no raw Recall@k-vs-F1 comparison. The probe×model pass/fail matrix is the headline deliverable.

---

## 6. Verified facts (trust these over any other project file)

### 6.1 Literature (source-verified 2026-06/07)

| Topic | Correct fact |
|---|---|
| BEDTime authors | **Sen, Gottesman, Qiu, Bruss, Nguyen, Hartvigsen** (2509.05215). "Tutuncuoglu" is fabricated. |
| BEDTime datasets | **Five** per the current paper version (2509.05215**v3**, 2026-04-10): TRUCE-Stock, TRUCE-Synthetic, **TaxoSynth**, SUSHI, **and NICU-HR** (named in the abstract and comparison table; 46,843 pairs). *Corrected 2026-08-18 with Ranyi's sign-off (w1_verification_record.md): the earlier "NICU-HR is fabricated" note applied to an earlier paper version. Cite v3; say "one of the datasets is TaxoSynth", not "the fourth". SUSHI-Tiny precedent unaffected (v3 still cites the Tiny Zenodo record).* |
| Tan et al. §4.4 | **Three** shuffles + **separate** masking perturbation. Mechanics pinned from paper + authors' code 2026-08-09: all perturbations **test-time only**; shuffles move **individual time steps** (sf-all whole sequence; sf-half first half only; **ex-half deterministic** half-swap, no randomness); masking sets random non-contiguous positions to **0**; channels shuffle **jointly** (same permutation, `[:, perm, :]`); one permutation shared per *batch*, single unseeded draws (global seed 2021 only). Code also contains an **unreported block-level shuffle (`sf_patchs`**, patch sizes 8–64 swept). ⚠ Unpinned: which mask ratio the paper's Masking column reports (code sweeps 0.0–0.8; shuffle-loop default 0.2) and which metric (MAE/MSE) underlies its % cells. `ablUtils.py` verified byte-identical across all three harnesses (sha256 4e0552…a6f0); only the OFA call path read. |
| Fons et al. Table 1 | **Seven** univariate categories (incl. **stationarity**) + 3 multivariate. EMNLP 2024. |
| ARO | Near-chance is on **relations** (VG-R ~59%); attribution (VG-A) ~62%. ICLR 2023 Oral. |
| MMTS-Bench | Prefix ablation = their own ChatTS-style reproduction (Qwen2.5-3B): ON 0.59 → OFF 0.24 → **OFF\* ≈0.60**. Shortcut audit is **dataset-level**. ⚠ ">0.95 Align ceiling" and "240 QA pairs" **unverified**. |
| TS-Haystack | Authors **Zumarraga et al.** (verified). **v4 (2026-04): 4 datasets / 4 modalities.** Venue "ICLR 2026 workshop" unverified. |
| TRACE constants | CIT; RoPE on patches not CIT; K=32; λ_ch=1.0; NOAA 74,337; P@1 44.10%; App. B.3 human-vs-ChatGPT split. |
| ChatTS details | Qwen2.5-14B-Instruct; value-preserved normalisation (§3.4.2); Dataset A 0.889/0.788; RQ5 noise inversion. |

### 6.2 CLaSP paper — read from source 2026-07-23 (arXiv:2411.08397v3)

- Specified: two separate encoders (§III.A, Eq. 1); learnable linear projections to common dim *d* (Eq. 2); C = τ·(Eₜ·Eₛᵀ) (Eq. 3); L = 0.5(ℓₜ+ℓₛ) with per-axis cross-entropy (Eqs. 4–6); joint training (§III.C); **Informer** signal encoder trained from scratch and **T5-Small** (`google-t5/t5-small`) text encoder (§IV.A); 8:1:1 splits; TRUCE length 12, SUSHI length 2048; mAP@10 with an independent judge encoder above threshold (§IV.B).
- SUSHI class labels have **three categories: Trend, Periodic, Fluctuation** (§IV.A) — the basis of the Probe-1 grammar.
- **Not specified anywhere:** *d*, τ, encoder depth/width, pooling, learning rates, batch size, epochs, optimiser, normalisation, split seed, judge variant, AP normalisation, **candidate pool per row**.
- Paper says TRUCE is "1,900" series; the actual public release is **2,460** (1,900 stock + 560 synthetic). Ours is ground truth.
- Table III **combined row is not a query-weighted mean** — demonstrated: the implied TRUCE query share differs across columns (5.3%, 3.4%, 6.4%, 2.2%) and is impossible for DistilBERT ts=0.5 (1.000, 1.000 → 0.959). It is a separate experiment, most plausibly a merged retrieval pool. **Excluded from comparison.**

### 6.3 Facts established by our own work (2026-07)

- **CLaSP has no public code release and no published checkpoint** (searched 2026-07). Our reimplementation is the object of study; authors contacted.
- **SUSHI public release is Tiny (1,400 signals)**; Base (~140K) is not publicly downloadable. BEDTime also uses Tiny — citable precedent. Tiny leaves **one test signal per class** under 8:1:1; Base would leave ~100, which is why the paper's SUSHI soft-mAP is structurally easier to reach.
- **Metric saturation:** DistilBERT at ts=0.5 accepts **99.7% of all 338,908 query–candidate pairs** in our pool; under such a judge mAP@10 → 1.0 for any ranking (0.997¹⁰ ≈ 0.97). The paper's 1.000 column is uninformative. This is primary evidence for the thesis premise.
- **Fidelity result:** on TRUCE (identical data), our reimplementation matches the paper across all four protocols within 0.059 (deviations −0.018, −0.031, −0.003, +0.059), reproducing both the strict-threshold collapse and the DistilBERT saturation. Over three seeds the published TRUCE value (0.458) **falls inside** our observed range (0.433–0.470).
- **Untrained control (2026-07-27):** a randomly initialised model scores 0.246 / 0.015 / **0.999** / 0.326 under the four protocols. Under the saturating protocol it marginally *exceeds* the trained model (0.997) — training changes that published number by nothing. Its score under each protocol closely tracks that judge's acceptance rate (29.4%→0.246, 2.6%→0.015, 99.7%→0.999, 37.7%→0.326).
- **SUSHI gap prediction confirmed:** gap widens with judge strictness (0.128 → 0.265), as predicted by Tiny-vs-Base test-pool composition.
- **C4 census-certified headline (2026-08-06):** on the 738 fluctuation-swap items individually certified as fair by a complete human census, CLaSP scores **0.603 [0.567–0.641]** vs 0.929–0.931 for the feature control and 0.969 for CLaSP on random distractors. Invalid items (125) score 0.531 — chance-like, confirming the census carved at a real joint.
- **SUSHI caption defect (2026-08-05/06):** a truncated "Large part," opener recurs in the smooth clause pool (12 instances found across sample + census) — a dataset caption-generation flaw; one limitations sentence.
- **TRUCE Probe-2 order-census (2026-08-13):** of 7,380 caption rows (5,087 unique texts), 96.6% of classified rows carry order-sensitive claims — certified invariant share 0.0334 (P2-8 registered ≥ 0.15, MISSED and recorded). With SUSHI (2.9%) and TRACE (0%), all three counted substrates are saturated with order language — candidate thesis finding. TRUCE data-quality defect for limitations: one literal '{}' caption ×7 rows plus scattered junk (a pasted dictionary definition), same family as SUSHI's "Large part," truncation.
- **TRUCE-synth test pool contains duplicate signals (verified 2026-08-13):** exact z-norm clusters {165,326,360} and {58,86}, plus one-float32-ulp near-duplicates 87↔165 and 249↔362; 15 exact-cluster queries (18 tied at runtime); SUSHI min nearest-neighbour distance 0.124. Consequence: ~2% of TRUCE R@1 is a tie coin-flip for ANY model, and the July frozen TRUCE R@1 contains 3 lucky flips (mechanism of the 2026-08-13 G6 stop under torch 2.13). Rank metric is now deterministic average rank (D2, PROJECT_CONTEXT). Data-quality note alongside the '{}' captions.
- **CLaSP Probe-2 headline (scored 2026-08-13):** shuffling destroys 66–78% of dependent-group MRR on both substrates; the TRUCE invariant group is TOST-flat in 2/3 checkpoints; masking barely moves TRUCE (n.s. in seed 44) while shuffling devastates it — CLaSP-TRUCE retrieval is order-driven, not value-driven (masking = 2/12 points, stated caveat); ex-half ≈ sf-all in severity.
- **BLAS column-path tie splitting (2026-08-14):** `C @ S.T` computes different output columns through different float paths, splitting bitwise-identical pool vectors at ~1e-7 — float-equality found 22 of the floor's 24 construction-forced tied queries (`scripts/diagnose_floor_ties.py`; registered loop-24/matmul-22 prediction hit exactly). Closed by the D2-F amendment (identity-level tie detection, floor arm; PROJECT_CONTEXT). Sibling of the torch-version sort-order finding; CLaSP's recorded tie counts are float-path-dependent under D2's standing fragility footnote.
- **The serialization-level duplicate family is larger than the float-level one (2026-08-14):** integer quantisation merges the TRUCE-synth pool into two identical-string groups of four ({58,86,249,362}, {87,165,326,360}) — 24 tied queries vs CLaSP's 15–18. Registered cluster-structure prediction missed; membership union as predicted.
- **Floor DiD mimicry (2026-08-14, named finding):** the floor's TRUCE DiD reproduces CLaSP-P2-2's sign, magnitude range, and arm pattern (+0.010/+0.084/+0.072; CIs exclude 0 in arms 43/44) from thin-cell noise on a no-capability model, with the opposite composition (invariant-driven improvement vs dependent-driven degradation). Partly structural: M3 seeds share permutation draws across models (a deliberate comparability feature — state it in the write-up).
- **SUSHI fluctuation vocabulary is class-exclusive** (e.g. "step" in 0/234 non-step sawtooth/square captions, 90% of step-class captions); probe items inherit a corpus-vs-plain-language semantics ambiguity wherever class terms have broader everyday readings. The stricter plain-language standard governed all validation judgments.

---

## 7. Compute & platform plan

- **CLaSP-scale work:** Colab Pro (purchased; 100 compute units, T4 ≈ 2 units/hour). Baseline training = ~14 min / ~0.5 units. Colab is used **via the official Google Colab VS Code extension**, so the IDE stays constant; code lives in the git repo and is cloned into the Colab runtime, with `pairs.jsonl` carried via Google Drive.
- **Local laptop:** sufficient for all evaluation, probe generation, analysis, and TRACE-scale models. **Not** sufficient for training on length-2048 signals (established empirically).
- **ChatTS:** 14B **fp16** (the released checkpoint's own dtype — the earlier "bf16" note corrected 2026-08-15 from the pinned config) ⇒ 1×A100 40GB-class, inference-only, full precision (quantisation excluded). Revised budget after local prep: **~3–5 h, ~€15–40** (logit readout = one forward pass per question; 31,356 questions; smoke-stage projection scored against the registered 1–3 h compute band). Runbook: `docs/chatts_gpu_runbook.md`. Colab is explicitly *not* the plan here — it cannot guarantee hardware. Institutional cluster preferred (asked, awaiting reply); rented pod via VS Code Remote-SSH is the accepted fallback (~€50–150). **EXECUTED 2026-08-16 on the rented pod: ≈0.44 h inference for all 31,356 questions (16.6–24 q/s; session logs committed) — well inside every budget line; the compute-band ⟦E-time⟧ registered as 1–3 h MISSED (overpadded per-forward estimate), recorded.**
- **Reproducibility discipline:** every experiment is a config-driven script writing JSON to `results/`; no notebooks for runs; seeds fixed and recorded; checkpoints kept out of Git (local + Drive).

---

## 8. Execution phases

- **Phase 0 — Paperwork:** ✅ complete.
- **Phase 1a — CLaSP baseline:** ✅ **complete.** Three seeds trained and aggregated, fidelity validated across four protocols, untrained control run. Optional remainder: a one-page baseline report.
- **Phase 1b — Remaining model baselines:** 🟨 **text-embedding-3-large ✅ complete 2026-08-09** (strict protocol identical to CLaSP harness B; MRR 0.027 vs chance 0.017 — floor confirmed; SUSHI below chance, mechanism hypothesis recorded as inference, accepted-and-footnoted). TRACE covered by the demo reproduction (P@1 0.42, orientation-level). **ChatTS baseline = the viability cells (SUSHI 0.726 / TRUCE 0.622 WEAK-VIABLE, 2026-08-16)** — Phase 1b closed.
- **Phase 2 — Probe 1 (component swap):** ✅ **complete on all four models 2026-08-16** (ChatTS: C2 collapse, C4 partial, C3 VOID; the cross-model matrix now exists — the paradigm-level claim is posable). Optional remainder: TRUCE substrate. Machinery built and validated; **CLaSP arm complete and fully hardened** (per-pair analysis, restricted control, complete item census — C4 headline 0.603 census-certified); floor baseline complete (VOID). **TRACE arm complete 2026-08-09** (N3 census-certified; gap +0.289 ± 0.007). Remaining: ChatTS, TRUCE substrate (optional). Do **not** describe Phase 2 as complete until the cross-model matrix exists — the paradigm-level claim depends on it.
- **Phase 3 — Probe 2 (shuffle):** ✅ **complete on all four models 2026-08-16** (ledger P2-1/2/3/4/6/7/9 confirmed, P2-5/8 missed with mechanisms; ChatTS adds the two-level patch finding and the PJ-defended masking cells). *(Box corrected 2026-08-15 — had been left ⬜ after execution; drift, not a status change.)*
- **Phase 4 — Probe 3 (summary-stats):** ✅ **complete on all four models 2026-08-16** (P3-4/7/2a/3 confirmed; P3-1/2b/2c/5/6 missed with mechanisms; ChatTS: no shortcut — every sub-order rung at chance, five-number inert). *(Box corrected 2026-08-16 — the rev. 16 paste pack omitted §8; drift caught by Ranyi, not a status change.)*
- **Phase 5 — Analysis and writing:** ⬜ statistics, cross-probe synthesis matrix, thesis text. Background and methods chapters can be drafted during Phases 2–4; `clasp_reimplementation_validation.md` and `finding_metric_saturation.md` are already thesis-ready material.

---

## 9. Defense preparation (fold into thesis defense prep)

- CLaSP = **well-controlled representative** of the plain dual-encoder paradigm, not SOTA ("representative", never "state-of-the-art").
- Why exactly three probes: top-down decomposition of caption content — compositional / sequential / quantitative.
- Prior papers give *circumstantial* evidence; this thesis gives *systematic measurement*.
- "Maybe purpose-built retrievers don't have these shortcuts" → ARO precedent; and small degradation is a finding, not a failure (TOST makes it claimable).
- Distractor-difficulty confound → differential-across-components signature.
- Probe-2 split validity → 3-way classification + human validation; diagnostic is the interaction, not the main effect.
- **Reimplementation validity** → the five-level ladder in `docs/clasp_reimplementation_validation.md`; the closing argument is that probes measure *relative* degradation against each model's own baseline, so conclusions hold at the level of a model class and are carried by the four-model matrix rather than any single baseline.
- **The thesis premise is evidenced, not assumed** → the metric-saturation finding was encountered in the first system reproduced, before any probe was run.
